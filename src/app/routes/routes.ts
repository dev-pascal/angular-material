import { AUTH_PATH, HOME_PATH } from "../paths/paths";

export const APP_ROOT_ROUTE = "/";

export const HOME_ROOT_ROUTE = APP_ROOT_ROUTE;
export const HOME_ROUTE = HOME_ROOT_ROUTE + HOME_PATH; 

export const AUTH_ROOT_ROUTE = APP_ROOT_ROUTE;
export const AUTH_ROUTE = AUTH_ROOT_ROUTE + AUTH_PATH;

